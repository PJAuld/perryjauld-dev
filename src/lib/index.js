/**
 * Library Index
 * 
 * This is the main entry point for the $lib alias in SvelteKit.
 * Place reusable components, utilities, and assets in this folder
 * to make them accessible via the $lib import alias throughout
 * the application (e.g., import { something } from '$lib/...').
 * This keeps imports clean and prevents relative path issues.
 */
